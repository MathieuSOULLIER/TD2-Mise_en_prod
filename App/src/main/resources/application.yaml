server:
  port: 8080

liquibase:
  enabled: false

datasource:
  url: jdbc:postgresql://postgres:5432/covid-docker
  username: postgres
  password: postgres

jpa:
  database-platform: org.hibernate.dialect.PostgreSQLDialect
  show-sql: true
  hibernate:
    ddl-auto: create

logging:
  level:
    org:
      springframework:
        security: TRACE
